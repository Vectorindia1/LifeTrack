<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose does the real theming (core/ui/theme). This XML theme only needs to
      give the window a sane background and no action bar before Compose draws.
    -->
    <style name="Theme.LifeTrack" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
